import * as React from "react";

import {StyledMenuButton, StyledMenuBar} from './styled-components';

export default () => (
  <StyledMenuButton>
    <StyledMenuBar />
    <StyledMenuBar />
  </StyledMenuButton>
)
